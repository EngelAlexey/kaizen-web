---
import Brand from "./Brand.jsx";
import { useTranslations } from "../../utils/i18n";

interface Props {
    lang?: "es" | "en";
}

const { lang = "es" } = Astro.props;
const t = useTranslations(lang);
---

<footer class="bg-background border-t border-border pt-16 pb-8 px-6 text-sm">
    <div class="max-w-7xl mx-auto grid md:grid-cols-4 gap-12 mb-12">
        <div class="col-span-1 md:col-span-2">
            <div class="mb-6">
                <Brand client:visible lang={lang} />
            </div>
            <p class="text-muted max-w-sm mb-6">
                {t("footer.desc")}
            </p>
        </div>

        <div>
            <h4 class="font-bold text-foreground mb-4">
                {t("footer.solutions")}
            </h4>
            <ul class="space-y-3 text-muted">
                <li>
                    <a
                        href={lang === "es"
                            ? "/es/soluciones#asistencias"
                            : "/en/solutions#asistencias"}
                        class="hover:text-primary transition-colors"
                        >{
                            lang === "es"
                                ? "Control de asistencia"
                                : "Attendance Control"
                        }</a
                    >
                </li>
                <li>
                    <a
                        href={lang === "es"
                            ? "/es/soluciones#rrhh"
                            : "/en/solutions#rrhh"}
                        class="hover:text-primary transition-colors"
                        >{
                            lang === "es"
                                ? "Recursos Humanos"
                                : "Human Resources"
                        }</a
                    >
                </li>
                <li>
                    <a
                        href={lang === "es"
                            ? "/es/soluciones#actividades"
                            : "/en/solutions#actividades"}
                        class="hover:text-primary transition-colors"
                        >{
                            lang === "es"
                                ? "Control de actividades"
                                : "Activity Control"
                        }</a
                    >
                </li>
                <li>
                    <a
                        href={lang === "es"
                            ? "/es/soluciones#ssoma"
                            : "/en/solutions#ssoma"}
                        class="hover:text-primary transition-colors"
                        >{
                            lang === "es"
                                ? "Salud y Seguridad"
                                : "Health & Safety"
                        }</a
                    >
                </li>
            </ul>
        </div>

        <div id={lang === "es" ? "contacto" : "contact"}>
            <h4 class="font-bold text-foreground mb-4">
                {t("footer.contacts")}
            </h4>
            <ul class="space-y-3 text-muted">
                <li>+506 8767 2300</li>
                <li>
                    <a
                        href="mailto:contacto@kaizenapps.net"
                        class="hover:text-primary">contacto@kaizenapps.net</a
                    >
                </li>
                <li class="pt-2">{t("footer.location")}</li>
            </ul>
        </div>
    </div>

    <div
        class="max-w-7xl mx-auto border-t border-border pt-8 text-center text-muted text-xs"
    >
        &copy; {new Date().getFullYear()} Kaizen Apps – {t("footer.rights")}
    </div>
</footer>
